import React from 'react'


export const Header = () => {


    return (
        <div className="Header">
       this is the header. make a reusable topbar to put here with an image, title, links.
        </div>
    )
}